import { EnvironmentInterface } from '@environments/environment.interface';

export const environment: EnvironmentInterface = {
  production: false,
  apiUrl: 'https://placeholder.com',
  integrationSettingsClientId: 'clientid',
};
